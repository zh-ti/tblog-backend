<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tian.tblog.mapper.ManagerMapper">
    <select id="queryManager" parameterType="String" resultType="Manager">
        select * from manager where id=#{id}
    </select>

    <select id="verifyManager" parameterType="map" resultType="Manager">
        select * from manager where account=#{account} and password=#{password}
    </select>

    <update id="updateManager" parameterType="map">
        update manager set account=#{account}, password=#{password}, position=#{position} where id=#{id}
    </update>

    <delete id="deleteManager" parameterType="String">
        delete from manager where id=#{id}
    </delete>

    <insert id="addManager" parameterType="map">
        insert into manager(id, account, password, position)
        values(#{id}, #{account}, #{password}, #{position})
    </insert>
</mapper>